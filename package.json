{
  "name": "query-viz-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test:unit": "bun test apps packages",
    "test:e2e": "playwright test",
    "build:demos": "bun run --cwd apps/demo build && bun run --cwd apps/piql-demo build && bun run --cwd apps/workbench-demo build",
    "check:budgets": "bun scripts/check-bundle-budgets.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@biomejs/biome": "^2.3.13",
    "@playwright/test": "^1.58.1",
    "bun-types": "^1.3.8"
  }
}
